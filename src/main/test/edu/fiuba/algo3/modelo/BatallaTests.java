package edu.fiuba.algo3.modelo;
/*
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.Spy;
import org.mockito.junit.MockitoJUnitRunner;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import static org.mockito.Mockito.doReturn;
import static org.mockito.Mockito.when;
@RunWith(MockitoJUnitRunner.class)
public class BatallaTests {

    @Spy
    private Pais atacante;
    @Spy
    private Pais defensor;

    public BatallaTests() {
        this.atacante = new Pais("Argentina", null, null);
        this.defensor = new Pais("Brasil", null, null);
    }

    @Test
    void PaisConquistaAOtro() {
        List<Integer> dadosAtacante = new ArrayList<>(Arrays.asList(4,3,2));
        doReturn(dadosAtacante).when(atacante).obtenerDadosAtacante();
        List<Integer> dadosDefensor = new ArrayList<>(Arrays.asList(2,1,1));
        //when(defensor.obtenerDadosDefensor()).thenReturn(dadosDefensor);

        Batalla.realizarAtaque(atacante, defensor);

    }
}
*/